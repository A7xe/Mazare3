# Prisma commands load `../../.env` (repository root).
#
#   cp .env.example .env          (from repo root)
#   pnpm db:migrate
#   pnpm db:seed
#
# Do not create packages/db/.env for normal local work.
# See ../../.env.example — DATABASE_URL is required there.
