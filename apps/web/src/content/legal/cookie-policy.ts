import { getLaunchPublicLegalPage } from '@mazare3/shared';
import type { LegalDocument } from '@/lib/legal/types';

export const cookiePolicyEn: LegalDocument = getLaunchPublicLegalPage('cookie-policy', 'en');
export const cookiePolicyAr: LegalDocument = getLaunchPublicLegalPage('cookie-policy', 'ar');
