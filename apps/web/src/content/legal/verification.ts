import { getLaunchPublicLegalPage } from '@mazare3/shared';
import type { LegalDocument } from '@/lib/legal/types';

export const verificationEn: LegalDocument = getLaunchPublicLegalPage('verification', 'en');
export const verificationAr: LegalDocument = getLaunchPublicLegalPage('verification', 'ar');
